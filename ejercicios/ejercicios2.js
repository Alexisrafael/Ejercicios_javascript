const countries = require('../json/ciudades.json')

function verificar_si_el_pais_existe(params) {
    // Verificar que el pais ingresado existe en nuestra base de datos.
    /* Todo texto suministrado siempre empiece con letra mayúscula */
    // Si el país existe retornar 'El país suministrado existe en nuestra base de datos.'
    // Si no existe retornar 'El país suministrado no existe en nuestra base de datos.'
}

function mostrar_las_ciudades_viculadas_al_pais(params) {
    // Verificar que el pais incresado existe en nuestra base de datos.
    // Si el país existe retornar la información de cada ciudad.
    // Si no existe retornar 'El país suministrado no existe en nuestra base de datos.'
}

function multiplica_dos_numeros_sin_usar_el_operador_matematico(num1, num2) {
    // Se debe realizar una multiplicacion sin usar el operador matematico
    // Y que el campo este limitado a numeros con 3 digitos
    // si ingresan numeros mayores a 3 digitos mostrar en pantalla 'No puede ingresar numeros con mas de 3 digitos'
    
}

module.exports = { verificar_si_el_pais_existe, mostrar_las_ciudades_viculadas_al_pais,  }